<form method="post" action="{{ path('app_courses_lesson_delete', {'id': lesson.chapter.courses.id, 'cid': lesson.chapter.id, 'rid': lesson.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lesson.id) }}">
    <button class="btn btn-danger btn-sm rounded"><i class="fas fa-trash"></i> Delete</button>
</form>
