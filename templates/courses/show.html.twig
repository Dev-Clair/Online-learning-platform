{% extends 'base.html.twig' %}

{% block title %}Courses{% endblock %}

{% block body %}
    <div class="container scrollable-container py-2">
        <h4>Courses</h4>

        <table class="table table-striped table-bordered mt-4">
            <tbody class="thead-dark text-center">
                <tr>
                    <th>Id</th>
                    <td>{{ course.id }}</td>
                </tr>
                <tr>
                    <th>Title</th>
                    <td>{{ course.title }}</td>
                </tr>
                <tr>
                    <th>Description</th>
                    <td>{{ course.description }}</td>
                </tr>
                <tr>
                    <th>Duration</th>
                    <td>{{ course.duration }}</td>
                </tr>
                <tr>
                {% if app.user and 'ROLE_INSTRUCTOR' in app.user.roles %}
                    <th>CreatedAt</th>
                    <td>{{ course.createdAt ? course.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                </tr>
                <tr>
                    <th>UpdatedAt</th>
                    <td>{{ course.updatedAt ? course.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                </tr>
                {% endif %}
            </tbody>
        </table>

        {% if app.user and 'ROLE_INSTRUCTOR' in app.user.roles %}
        <div class="mb-2 btn-group float-end">
            <a class="btn btn-secondary rounded btn-sm" href="{{ path('app_courses_index') }}"> <<< </a>

            <a class="btn btn-info rounded btn-sm" href="{{ path('app_courses_edit', {'id': course.id}) }}"><i class="fas fa-edit"></i>
            </a>

            {{ include('courses/_delete_form.html.twig') }}
        </div>
        {% endif %}
    </div>
{% endblock %}
