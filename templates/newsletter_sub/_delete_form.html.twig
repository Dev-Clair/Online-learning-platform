<form method="post" action="{{ path('app_newsleter_sub_delete', {'id': newsleter_sub.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ newsleter_sub.id) }}"><i class="fas fa-trash"></i></button>
</form>
