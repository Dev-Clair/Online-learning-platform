{% extends 'base.html.twig' %}

{% block body %}
        <div class="container py-5 h-100">
            <div class="row d-flex justify-content-center align-items-center h-100">
                <div class="col-12 col-md-8 col-lg-6 col-xl-5">
                    <div class="card bg-light text-white" style="border-radius: 1rem;">
                        <div class="card-body p-5 text-center">
                            <div class="mb-4">
                              <h5 class="fw-bold mb-5 text-uppercase text-primary d-flex justify-content-center align-items-center">Update Profile</h5>

                              {{ form_start(form, {'attr': {'id': 'form-edit-profile'}}) }}

                              {% if form_errors(form) %}
                                  <div class="alert alert-danger">
                                      {{ form_errors(form) }}
                                  </div>
                              {% endif %}

                              <div class="mb-4 text-primary">
                                  {{ form_row(form.dateOfBirth, {'attr': {'disabled': false}}) }}
                              </div>

                              <div class="mb-4 text-primary">
                                  {{ form_row(form.country, {'attr': {'disabled':false}}) }}
                              </div>

                              <div class="mb-2">
                                  <button class="btn btn-success btn-sm px-5" type="submit">Update</button>
                              </div>

                              {{ form_end(form) }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
{% endblock %}
